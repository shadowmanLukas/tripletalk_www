diff --git a/storage.rules b/storage.rules
--- a/storage.rules
+++ b/storage.rules
@@ -7,12 +7,16 @@ service firebase.storage {
       return request.auth != null;
     }
 
+    function isAdmin() {
+      return signedIn() && request.auth.token.admin == true;
+    }
+
     function owns(userId) {
       return signedIn() && request.auth.uid == userId;
     }
 
     match /feedback/{userId}/{feedbackId}/{fileName} {
-      allow read, delete: if owns(userId);
+      allow read, delete: if owns(userId) || isAdmin();
       allow create, update: if owns(userId) &&
         fileName.matches('attachment_[1-3]\\.(jpg|png|gif|webp|heic|heif)') &&
         request.resource.size <= 3 * 1024 * 1024 &&
